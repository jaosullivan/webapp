# The active workflow definition lives at:
#   .github/workflows/bootstrap-argocd.yml  (repo root — required by GitHub Actions)
#
# This file is kept here for project navigation. Any changes must be
# made to .github/workflows/bootstrap-argocd.yml, not this file.
#
# Trigger: workflow_dispatch (manual, one-time cluster bootstrap)
# Steps: install ArgoCD → apply AppProject → apply Application
