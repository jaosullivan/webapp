# The active workflow definition lives at:
#   .github/workflows/ci.yml  (repo root — required by GitHub Actions)
#
# This file is kept here for project navigation. Any changes must be
# made to .github/workflows/ci.yml, not this file.
#
# Pipeline: test (pytest + PostgreSQL) → build-push (ghcr.io) → update-manifests (Kustomize)
